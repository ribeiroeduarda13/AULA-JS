let média = 6
let faltas = 2

if (média >= 7 && faltas <= 5) {
    console.log(`Jovem APROVADO com média: ${média} e faltas: ${faltas}`);
} else if (média >= 6 && faltas <= 5) {
    console.log(`jovem Ap. Conselho com média: ${média} e faltas: ${faltas}`);
} else {
    console.log(`Jovem REPROVADO com média: ${média} e faltas: ${faltas}`);
}